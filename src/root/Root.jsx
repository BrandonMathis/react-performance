import React, { Component } from 'react';
import Grid from '../grid/Grid';

class Root extends Component {
  render() {
    return (
      <div>
        <Grid x={20} y={20}/>
      </div>
    );
  }
}

export default Root;
